/**
 * Success Message for all prxoy api
 */
const successMessage = {
    login: "You have been logged in successfully.",
  
}

/**
 * Error message for all proxy api and form validation
 */
const errorMessage = {
    common: "Something went to wrong, Please try again",
    usernameRequired: "username is required.",
    passwordRequired: "password is required."
}

/**
 * status code of http request
 */
const apiStatusCode = {
    sucessStatus: 200,
    errorStatus: 400
}


export {successMessage, errorMessage, apiStatusCode}